{% extends 'base.html.twig' %}

{% block body %}

    <h1>{{ "Registration on the website" | trans }}</h1>

    {{ form_start(form) }}

    {{ form_widget(form) }}

    <button type="submit" class="btn btn-success">{{ "Registration" | trans }}</button>

    {{ form_end(form) }}

{% endblock %}